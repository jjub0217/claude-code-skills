# Claude Code Skills — 스프린트별 테스트 시나리오

---

## Sprint 1: bug-fix 범용화

### TC-1.1: 설정 섹션 존재 확인
1. `skills/bug-fix/SKILL.md` 파일을 연다
2. 상단에 `## 설정` 섹션이 있는지 확인
3. 필수(GitHub 레포, base 브랜치)와 선택(Notion DB ID 등)이 구분되어 있는지 확인
- **기대결과**: 설정 섹션이 명확하게 구분되어 있음

### TC-1.2: 하드코딩 제거 확인
1. `skills/bug-fix/SKILL.md`에서 `jjub0217/cuddle-market` 검색
2. `30ff2b30-` 검색
3. `7c32774b-` 검색
- **기대결과**: 본문에 하드코딩된 값이 없고, 설정 섹션의 예시에만 존재하거나 플레이스홀더(`{설정의 ...}`)로 대체됨

### TC-1.3: Notion fallback 로직 존재 확인
1. `skills/bug-fix/SKILL.md`에서 "Notion 미설정 시" 또는 "로컬 fallback" 검색
2. Step 2, Step 3, Step 6에 각각 분기가 있는지 확인
3. `qa/` 폴더 경로가 명시되어 있는지 확인
- **기대결과**: Notion 없을 때 로컬 마크다운으로 QA 티켓/Test Scenario 생성하는 분기가 존재

### TC-1.4: 전제 조건 변경 확인
1. `skills/bug-fix/SKILL.md` 상단의 전제 조건 확인
- **기대결과**: "Notion MCP 연결 필요" → "GitHub CLI (gh) 필수. Notion MCP는 선택사항." 으로 변경됨

### TC-1.5: 기존 Notion 로직 보존 확인
1. Notion 설정이 있을 때의 Step 2~3 로직이 여전히 존재하는지 확인
2. `notion-create-pages`, `notion-update-page` 등 도구 사용법이 유지되는지 확인
- **기대결과**: Notion 설정 시 기존과 동일하게 동작하는 로직이 보존됨

---

## Sprint 2: daily-scrum + context-sync 범용화

### TC-2.1: daily-scrum 설정 섹션 확인
1. `skills/daily-scrum/SKILL.md`에 `## 설정` 섹션 존재 확인
2. Notion DB ID가 설정으로 분리되었는지 확인
- **기대결과**: 설정 섹션에 Notion DB ID가 선택 항목으로 존재

### TC-2.2: daily-scrum Notion fallback 확인
1. "Notion 미설정 시" 분기 존재 확인
2. `scrum/` 폴더에 마크다운 생성하는 로직 확인
- **기대결과**: Notion 없을 때 `scrum/YYYY-MM-DD-daily-scrum.md` 생성

### TC-2.3: context-sync 설정 섹션 확인
1. `skills/context-sync/SKILL.md`에 `## 설정` 섹션 존재 확인
2. Notion DB ID, GitHub 레포명이 설정으로 분리되었는지 확인
- **기대결과**: 필수(GitHub 레포)와 선택(Notion DB) 구분

### TC-2.4: context-sync Notion fallback 확인
1. Notion 미설정 시 GitHub 수집 + 싱크 문서만 생성하는 로직 확인
2. Daily Scrum 생성 단계에서 로컬 fallback 확인
- **기대결과**: Notion 없어도 `sync/` 문서 생성 + `scrum/` 문서 생성

### TC-2.5: 하드코딩 제거 확인
1. 두 파일에서 `jjub0217`, `30ff2b30-`, `cuddle-market` 검색
- **기대결과**: 본문에 하드코딩 없음 (설정 예시에만 존재)

---

## Sprint 3: create-issue + commit-push 범용화

### TC-3.1: create-issue 설정 섹션 확인
1. `skills/create-issue/SKILL.md`에 브랜치 네이밍 컨벤션이 설정으로 분리되었는지 확인
2. 라벨 매핑(FEAT, FIX 등)이 설정으로 분리되었는지 확인
- **기대결과**: 브랜치 형식, 라벨 매핑이 설정 섹션에 존재

### TC-3.2: create-issue 이슈 템플릿 설정화 확인
1. 이슈 템플릿 경로가 설정으로 분리되었는지 확인
- **기대결과**: `.github/ISSUE_TEMPLATE/` 경로가 설정 가능

### TC-3.3: commit-push PR 템플릿 설정 확인
1. `skills/commit-push/SKILL.md`에 base 브랜치가 설정으로 분리되었는지 확인
2. PR body 형식(chore/fix/feat별)이 설정으로 분리되었는지 확인
- **기대결과**: base 브랜치, PR 형식이 설정 가능

### TC-3.4: 하드코딩 제거 확인
1. 두 파일에서 `develop`, `jjub0217`, 프로젝트 전용 라벨 검색
- **기대결과**: 설정 예시에만 존재

---

## Sprint 4: 학습/유틸리티 스킬 범용화

### TC-4.1: learn-log 경로 설정화 확인
1. `skills/learn-log/SKILL.md`에서 히스토리 파일 경로가 설정으로 분리되었는지 확인
2. 학습 로그 파일 경로(`~/Desktop/weekly-learning-log.md`)가 설정으로 분리되었는지 확인
- **기대결과**: 경로가 설정 섹션에 존재, 본문에서는 `{설정의 ...}` 사용

### TC-4.2: blog-write 경로 설정화 확인
1. `skills/blog-write/SKILL.md`에서 학습 로그 파일 경로가 설정으로 분리되었는지 확인
- **기대결과**: 학습 로그 경로가 설정 가능

### TC-4.3: study-roadmap 범용성 확인
1. `skills/study-roadmap/SKILL.md`에 프로젝트 종속 정보가 있는지 확인
- **기대결과**: 범용적이거나, 종속 정보가 설정으로 분리됨

### TC-4.4: my-session-wrap, my-trouble-detect 확인
1. 두 파일에 프로젝트 종속 정보(경로, DB ID 등)가 있는지 확인
- **기대결과**: 범용적이거나, 종속 정보가 설정으로 분리됨

### TC-4.5: 전체 5개 파일 하드코딩 검색
1. 5개 파일 모두에서 `osejin`, `cuddle-market`, `Desktop` 등 개인/프로젝트 경로 검색
- **기대결과**: 설정 예시에만 존재

---

## Sprint 5: refactor-scan + README + 최종 정리

### TC-5.1: refactor-scan 범용화 확인
1. `skills/refactor-scan/SKILL.md`에 프로젝트 종속 규칙이 설정으로 분리되었는지 확인
- **기대결과**: React Best Practice 규칙은 유지, 프로젝트 종속 부분만 설정화

### TC-5.2: 루트 README 업데이트 확인
1. `README.md`에 각 스킬별 설정 가이드가 보강되었는지 확인
2. 사용 예시가 추가되었는지 확인
- **기대결과**: 설치 → 설정 → 사용 흐름이 명확

### TC-5.3: 개별 README 존재 확인
1. 각 스킬 폴더(`skills/*/`)에 `README.md`가 있는지 확인
2. 스킬별 상세 설명 + 설정 방법이 포함되어 있는지 확인
- **기대결과**: 11개 스킬 폴더 모두에 README.md 존재

### TC-5.4: 전체 하드코딩 최종 점검
1. 전체 레포에서 다음 키워드 검색:
   - `jjub0217`
   - `cuddle-market`
   - `30ff2b30-`
   - `7c32774b-`
   - `/Users/osejin`
2. 설정 예시 외에 본문에 하드코딩이 없는지 확인
- **기대결과**: 하드코딩 0건 (설정 예시 제외)

### TC-5.5: 전체 스킬 설정 섹션 일관성 확인
1. 11개 스킬의 `## 설정` 섹션이 동일한 형식(필수/선택 테이블)을 따르는지 확인
- **기대결과**: 설정 형식이 모든 스킬에서 일관됨
